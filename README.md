# BehaveBot

Telegram bot that records trading behavior (no wallet, no trades, no signals).

## Run

1. Copy `.env.example` to `.env` and set `BOT_TOKEN` and `ADMIN_IDS`.
2. `pip install -r requirements.txt`
3. `python main.py`
